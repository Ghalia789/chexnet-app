apiVersion: v1
kind: Service
metadata:
  name: chexnet-service
  namespace: chexnet-prod
  labels:
    app: chexnet
    env: production
spec:
  type: LoadBalancer
  selector:
    app: chexnet
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 7860
